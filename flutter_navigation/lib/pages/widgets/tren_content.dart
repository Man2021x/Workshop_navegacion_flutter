import 'package:flutter/material.dart';

class TrenContent extends StatelessWidget {
  const TrenContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("El tren"));
  }
}