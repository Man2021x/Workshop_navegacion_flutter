import 'package:flutter/material.dart';

class BicicletaContent extends StatelessWidget {
  const BicicletaContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("La Bicicleta"));
  }
}